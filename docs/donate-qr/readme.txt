不要使用从仓库历史中发现的码，它们有可能过期了，而是使用 fayige.top 网页上显示的码，不然有可能丢失资金。

Do not use qr code got from repo history, they might have expired, but just use qr code displayed on the fayige.top webpage, otherwise the money might be lost.

